# Trivy vulnerability ignore file
# Known vulnerabilities that cannot be patched at the Node.js base image level

# Node.js 22-bookworm-slim known vulnerability
# This is a base OS-level vulnerability in the bookworm release
# It does not affect the application and is under active monitoring
